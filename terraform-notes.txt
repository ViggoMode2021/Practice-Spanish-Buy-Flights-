terraform workspace new stage 
    'command used to create a new workspace, a workspace is a deployment of a configuration'

lookup  
    'retrieves the value of a single element of a map, given its key. If the given key does not exist, the given default value is returned instead.

terraform apply -refresh-only 
    'this command reconciles the state Terraform knows about with the real-world infrastructure'

TF_LOG
    'enable detailed logs by setting the TF_LOG environment variable to any value'

Terraform 0.12
    'substantial changes to the HCL syntax were released in this version'

terraform workspace select dev
    'switch to dev workspace'

alias
    'meta-argument used when using the same provider with different configurations for different resources'




